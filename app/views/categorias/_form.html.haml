= form_for @categoria, :html => { class: 'form-horizontal', role: 'fordb/migrate/20131119172026_create_diseños.rbm' } do |f|
  - if @categoria.errors.any?
    #error_explanation
      %h2= "#{pluralize(@categoria.errors.count, "error")}"
      %ul
        - @categoria.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :nombre, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :nombre, class: 'form-control'
  .actions
    = f.submit 'Guardar', class: 'btn btn-default'
