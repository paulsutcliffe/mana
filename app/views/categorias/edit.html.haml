#region-categorias.container

  .region
    .region-title
      %h1= page_title!('Editando categoria')

  .region
    = render 'form'

  .region
    .admin-controls
      = link_to 'Ver', @categoria, class: 'btn btn-info'
      = link_to 'Regresar', 'javascript:history.back(-1)', class: 'btn btn-warning'
